workflows:
  android-workflow:
    name: Android Workflow
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - name: Free Withdraw Build
        script: |
          rm -rf android lib
          flutter create --platforms=android .
          flutter pub add url_launcher
          cat > lib/main.dart << 'DART'
          import 'dart:async'; import 'package:flutter/material.dart'; import 'package:url_launcher/url_launcher.dart';
          void main()=>runApp(MaterialApp(debugShowCheckedModeBanner:false,home:Home()));
          class Home extends StatefulWidget{State<Home> createState()=>_S();}
          class _S extends State<Home>{
            double b=0.01200; bool m=true; Timer? t; var wc=TextEditingController(); int adCount=0;
            String adLink = "https://www.effectivecpmnetwork.com/vm70uh6qxb?key=30472d3647946c4a2a73f4ef7660c55f";
            void initState(){super.initState(); t=Timer.periodic(Duration(seconds:2),(_){if(m)setState(()=>b+=0.00001);});}
            void _watchAd() async {
              await launchUrl(Uri.parse(adLink), mode: LaunchMode.externalApplication);
              await Future.delayed(Duration(seconds:1));
              if(!mounted) return;
              showDialog(context:context, builder:(_)=>AlertDialog(
                title:Text('Reward Ready!'), content:Text('Ad dekhne ke liye shukriya. Claim dabao.'),
                actions:[ElevatedButton(style:ElevatedButton.styleFrom(backgroundColor:Colors.green,minimumSize:Size(double.infinity,48)), onPressed:(){Navigator.pop(context); setState((){b+=0.00010; adCount++;}); ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(' +0.00010 BSC Added!')));}, child:Text('CLAIM +0.00010',style:TextStyle(color:Colors.white)))],
              ));
            }
            void _withdraw() {
              if(b < 0.05){ ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text('Minimum 0.05 BSC required. Keep mining & watching ads!'))); return; }
              showDialog(context:context, builder:(_)=>AlertDialog(
                title:Text('Free Withdraw'),
                content:Column(mainAxisSize:MainAxisSize.min, children:[
                  Text('Balance: ${b.toStringAsFixed(5)} BSC', style:TextStyle(fontWeight:FontWeight.bold)),
                 
