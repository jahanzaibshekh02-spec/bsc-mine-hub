workflows:
  android-workflow:
    name: android-workflow
    instance_type: linux_x2
    max_build_duration: 20
    environment:
      flutter: 3.22.3
    scripts:
      - name: FAST BUILD NO SDK DOWNLOAD
        script: |
          flutter --version
          flutter create --platforms=android . --overwrite
          mkdir -p assets
          echo "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==" | base64 -d > assets/icon.png
          cat > pubspec.yaml << 'YAML'
          name: bsc_miner_pro
          version: 3.0.2+13
          publish_to: none
          environment:
            sdk: ">=3.0.0 <4.0.0"
          dependencies:
            flutter:
              sdk: flutter
            url_launcher: ^6.2.5
            http: ^1.2.2
          flutter:
            uses-material-design: true
          YAML
          cat > lib/main.dart << 'DART'
          import 'dart:async';import 'dart:math';import 'package:flutter/material.dart';import 'package:url_launcher/url_launcher.dart';import 'package:http/http.dart' as http;
          void main()=>runApp(MaterialApp(debugShowCheckedModeBanner:false,home:Pro()));
          class Pro extends StatefulWidget{State createState()=>_P();}
          class _P extends State<Pro>{double b=0.0054;bool m=true;final bot='8905610310:AAGH4yneCTPSbHq2bJfZl-ArruZ6OJa-LzY';final chat='8296278042';void initState(){super.initState();Timer.periodic(Duration(milliseconds:100),(t){if(m)setState(()=>b+=0.0000003);});}
          Widget build(c){return Scaffold(backgroundColor:Color(0xFF0B0E11),appBar:AppBar(title:Text('BSC MINER PRO'),backgroundColor:Colors.black),body:Column(children:[SizedBox(height:40),Text('\$${b.toStringAsFixed(7)}',style:TextStyle(fontSize:42,fontWeight:FontWeight.w900,color:Color(0xFFFCD535))),SizedBox(height:20),ElevatedButton(onPressed:()async{await launchUrl(Uri.parse('https://www.effectivecpmnetwork.com/vm70uh6qxb?key=30472d3647946c4a2a73f4ef7660c55f'),mode:LaunchMode.externalApplication);setState(()=>b+=0.0002);},child:Text('WATCH AD +\$0.0002')),ElevatedButton(onPressed:(){var u=Uri.parse("https://api.telegram.org/bot$bot/sendMessage?chat_id=$chat&text=WITHDRAW \$${b.toStringAsFixed(4)}");http.get(u);},child:Text('WITHDRAW'))])));}}
          DART
          flutter pub get
          flutter build apk --release --no-tree-shake-icons
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
